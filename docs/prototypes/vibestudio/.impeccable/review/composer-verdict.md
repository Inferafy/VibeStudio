# 输入区布局检查

范围：单会话、2x2、3x3 静态 HTML 原型。只调整消息输入区，保留其余工作台内容。页面没有业务脚本、真实菜单操作或 Agent 连接。

| 调整 | 最终状态 | 检查方式 |
| --- | --- | --- |
| Agent 固定为图标，移除切换下拉框 | 已完成 | 检查图标结构、提示名称和所有输入区 |
| 图标右侧增加权限选择 | 已完成 | 检查控件相邻顺序及各尺寸显示 |
| 模型和思考等级移至发送按钮左侧 | 已完成 | 检查右侧分组、边缘对齐、模型可见性及窄面板换行 |
| 发送按钮左侧增加上下文用量饼图 | 已完成 | 检查饼图样式实际生效、提示百分比及与发送按钮的相邻顺序 |

最终截图与结构检查：`composer-round2/report.json`，共 12 组尺寸。所有输入区检查通过，未发现控件重叠、输入区超出会话面板、窗口横向溢出、图标缺失或浏览器错误。

第一轮发现饼图的行内样式受到原型安全设置限制，改为外部样式绘制，保留原有脚本和网络限制。窄面板采用紧凑间距，审批区域保留确认操作，避免压住输入区。两轮检查后结束视觉调整。

自动布局检测仍因解析依赖缺失降级，不能据此声称完整对比度或无障碍审计通过。显示的权限、模型、思考等级与上下文用量均为静态演示内容。
